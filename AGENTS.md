# Unit Calculator 開発指示

## 目的

Windows 11で日常的に使える、Windows標準電卓に近い雰囲気の単位変換・単位計算アプリを作る。
初版では機能を広げすぎず、起動の速さ、入力のしやすさ、エラーの分かりやすさを優先する。

## 技術方針

- Python 3.11以上を使用する。
- GUIはPython標準の`tkinter`と`tkinter.ttk`で実装する。
- 単位処理にはPintを使用する。
- 標準単位はPint内蔵定義を利用し、独自単位だけ`units.txt`から追加で読み込む。
- 開発時はインタプリタで直接実行できる状態を保つ。
- Windows向け配布はPyInstallerの`--onedir`を使用する。`--onefile`は使用しない。
- GUI以外のロジックは、画面を起動せずテストできるように分離する。

## 実装上の原則

- `eval()`、`exec()`および任意のPythonコードを実行できる仕組みを実装しない。
- 次元が異なる単位同士を暗黙に変換したり、エラーを無視したりしない。
- `10 m + 5 s`のような不正な計算には、日本語で理由が分かるエラーを返す。
- 温度のようなオフセット単位は、数値と単位を分けてQuantityを生成し、正しく変換する。
- `units.txt`が存在しない場合も、Pint内蔵単位だけで起動できるようにする。
- `units.txt`に不正な定義がある場合は、アプリを異常終了させず、警告を表示して標準単位で続行する。
- 数値の表示処理を一か所に集約し、不要な末尾ゼロを除去する。
- UIを完全にWindows電卓と複製する必要はない。余白、色、ボタン配置、結果表示の優先順位を参考にする。
- 現時点で必要のない抽象化、プラグイン機構、データベース、設定ファイルは追加しない。

## コード構成

初回実装では、少なくとも次のファイルに責務を分ける。

- `unit_calculator.py`
  - エントリーポイント
  - Tkinter画面
  - キーボード操作
  - 画面状態の管理
- `unit_engine.py`
  - Pintの初期化
  - 単位変換
  - 単位付き計算
  - 数値・単位の表示整形
  - GUIに依存しない例外変換
- `tests/test_unit_engine.py`
  - 計算エンジンの自動テスト

1ファイルが大きくなった場合にだけ追加分割する。最初から細かく分割しすぎない。

## UI方針

- 起動時は「単位変換」モードを表示する。
- 「単位変換」と「単位計算」の2モードを切り替えられるようにする。
- 単位選択欄は編集可能なコンボボックスにする。
  - よく使う単位は候補から選択できる。
  - Pintが認識する単位や`units.txt`の独自単位は直接入力できる。
- 入力値や式はキーボードでもボタンでも入力できるようにする。
- 計算結果は最も目立つ位置に表示し、コピーできるようにする。
- 入力途中のエラーはモーダルダイアログで連続表示せず、画面内のメッセージとして表示する。
- エラーが発生しても、入力内容を勝手に消さない。

## 品質基準

- 公開関数と複雑な処理には、目的が分かるdocstringを付ける。
- 型ヒントを使用する。
- UIに依存しない処理にはpytestによるテストを作る。
- 少なくとも`REQUIREMENTS.md`の受け入れ条件をテストする。
- 実装後は次を実行して結果を確認する。

```powershell
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\python.exe -m compileall unit_calculator.py unit_engine.py
```

## 変更時の注意

- 要件と異なる判断が必要になった場合は、黙って仕様を変えず、理由と選択肢を示す。
- ユーザーが作成した変更や無関係なファイルを上書きしない。
- 明示されていないGitのコミット、push、ブランチ作成は行わない。
- 実装後の報告には、変更内容、確認結果、既知の制約を簡潔に含める。
