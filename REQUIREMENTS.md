# Unit Calculator 要件定義

## 1. 概要

Windows 11上で動作する、単位変換を中心としたデスクトップ電卓を作成する。
単純な単位変換を主な用途とし、補助機能として単位付き計算式にも対応する。

## 2. 対象環境

- OS：Windows 11
- Python：3.11以上
- GUI：Tkinter / ttk
- 単位計算：Pint
- 配布方法：PyInstaller `--onedir`

## 3. MVPの対象機能

### 3.1 単位変換モード

- 起動時に表示する既定モードとする。
- 単位カテゴリを選択できる。
- 変換元と変換先の単位を選択または直接入力できる。
- 数値を入力すると変換結果を表示する。
- 変換元と変換先を入れ替えられる。
- 結果をクリップボードへコピーできる。
- 温度のようなオフセット単位を正しく変換できる。

初期カテゴリと候補単位は以下を目安とする。Pint上の正確な単位名へ対応付けること。

| カテゴリ | 候補 |
| --- | --- |
| 長さ | mm、cm、m、km、inch、foot、yard、mile |
| 面積 | mm²、cm²、m²、km²、hectare、acre |
| 体積 | mL、L、m³、US gallon |
| 質量 | mg、g、kg、t、ounce、pound |
| 時間 | ms、s、min、h、day |
| 速度 | m/s、km/h、knot、mph |
| 温度 | °C、°F、K |
| 圧力 | Pa、kPa、MPa、bar、psi |
| エネルギー | J、kJ、Wh、kWh |
| 電力 | W、kW、horsepower |

単位コンボボックスは編集可能とし、一覧にないPint対応単位も入力できるようにする。

### 3.2 単位計算モード

- 数値だけの通常計算と、単位を含む式を入力できる。
- 四則演算、括弧、整数の累乗に対応する。
- 出力単位を任意で指定できる。
- 出力単位が空の場合は、Pintが計算した単位で表示する。
- 次元の不一致を検出する。
- 次の入力補助を行う。
  - `×`を`*`として扱う。
  - `÷`を`/`として扱う。
  - `^`を累乗として扱う。

例：

```text
式: 51 m/s * 8 s
出力単位: km
結果: 0.408 km
```

### 3.3 独自単位

- 標準単位はPintの既定レジストリから読み込む。
- アプリと同じディレクトリにある`units.txt`を追加定義として読み込む。
- `units.txt`が空または存在しない場合も起動する。
- 独自単位は、単位選択欄への直接入力および単位計算モードで利用できる。
- 初版では独自単位をカテゴリ一覧へ自動分類しない。

読み込み方法は、標準定義を失わないように次の流れとする。

1. `pint.UnitRegistry()`で標準レジストリを作成する。
2. `units.txt`が存在する場合だけ`load_definitions()`で追加読み込みする。
3. 読み込みに失敗した場合は標準レジストリを作り直して続行する。

### 3.4 入力と操作

- マウス操作とキーボード操作の両方に対応する。
- `Enter`：変換または計算を実行する。
- `Escape`：現在の入力をクリアする。
- `Backspace`：式の末尾を1文字削除する。
- `Ctrl+C`：結果をコピーする。
- フォーカス移動は通常の`Tab`操作に従う。
- 数字、少数点、括弧、四則演算の画面ボタンを用意する。

## 4. 表示

- 日本語UIとする。
- 結果を入力値より大きく表示する。
- 不要な末尾ゼロを表示しない。
- 通常は最大12桁程度の有効数字で表示する。
- 極端に大きい値または小さい値では科学表記を使用してよい。
- エラーは画面内に日本語で表示する。
- 単位記号は可能な範囲で一般的な表示に整える。

## 5. エラー処理

以下を区別できるメッセージにする。

- 数値として解釈できない。
- 単位が存在しない。
- 変換元と変換先の次元が異なる。
- 計算式の構文が不正。
- 単位定義ファイルが不正。

入力途中にエラーが発生してもアプリを終了させない。

## 6. ファイル配置

開発時はスクリプトのあるディレクトリを基準とする。
PyInstallerで実行している場合は、実行ファイルのあるディレクトリを基準とする。
`units.txt`は常にその基準ディレクトリから探す。

## 7. 受け入れ条件

最低限、次を満たすこと。

1. `10 km`を`m`へ変換すると`10000 m`になる。
2. `0 °C`を`°F`へ変換すると`32 °F`になる。
3. `51 m/s * 8 s`を`m`で出力すると`408 m`になる。
4. `51 m/s * 8 s`を`km`で出力すると`0.408 km`になる。
5. `10 m + 5 s`は次元不一致のエラーになる。
6. 空の出力単位で`2 + 3 * 4`を計算すると`14`になる。
7. `units.txt`に`custom_count = 0.25 * millimeter = cnt`を定義した場合、`4 custom_count`を`mm`へ変換すると`1 mm`になる。
8. `units.txt`が存在しなくても起動できる。
9. 不正な`units.txt`があっても異常終了せず、標準単位を利用できる。
10. `pytest`によるエンジンのテストがすべて成功する。

## 8. MVPの対象外

- 計算履歴の永続保存
- お気に入り単位
- ユーザー設定ファイル
- 為替レート
- 変数代入
- 関数定義
- 高度な関数電卓機能
- 独自単位のカテゴリ自動判定
- 完全なWindows 11ダークモード追従
- インストーラー作成
- `--onefile`形式の配布

これらは初版の使用感を確認してから追加を判断する。
