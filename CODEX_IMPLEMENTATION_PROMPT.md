# Codexへ渡す最初の実装指示

以下の本文を、プロジェクトフォルダを開いた状態のCodexへそのまま渡してください。

---

このプロジェクトに、Windows 11向けの単位変換・単位計算アプリを実装してください。

最初に`AGENTS.md`、`REQUIREMENTS.md`、`README.md`をすべて読み、記載された方針と受け入れ条件に従ってください。実装前に既存ファイルを確認し、要件と矛盾する点があれば先に指摘してください。矛盾がなければ、途中で確認待ちにせず実装から検証まで進めてください。

## 実装するもの

- `unit_calculator.py`
  - Tkinter / ttkによるGUI
  - 起動時は単位変換モード
  - 単位変換モードと単位計算モードの切り替え
  - キーボード操作と画面ボタン
  - 結果コピー
  - 画面内の日本語エラー表示
- `unit_engine.py`
  - Pintレジストリの生成
  - `units.txt`の追加読み込み
  - 数値と単位を分けた単位変換
  - 単位付き式の評価
  - 出力単位への変換
  - 表示整形
  - GUIに依存しない日本語エラー
- `tests/test_unit_engine.py`
  - `REQUIREMENTS.md`の受け入れ条件を中心としたテスト

## 重要な制約

- Python 3.11以上を対象にしてください。
- GUIには標準ライブラリの`tkinter`と`tkinter.ttk`だけを使用してください。
- 単位処理にはPintを使用してください。
- `eval()`や`exec()`を直接使用しないでください。
- 標準単位を維持したまま、`UnitRegistry.load_definitions()`で`units.txt`を追加読み込みしてください。
- 温度変換では`0 * degC`のような乗算式を作らず、数値と単位からQuantityを生成してください。
- 単位選択用コンボボックスは編集可能にし、一覧にない単位も直接入力できるようにしてください。
- カスタム単位をカテゴリへ自動分類する機能は作らないでください。
- 設定ファイル、データベース、履歴永続化は追加しないでください。
- PyInstallerの`--onefile`対応は行わないでください。
- Windows標準電卓の完全な複製ではなく、結果の見やすさ、余白、ボタン配置を参考にしてください。
- 無関係なファイルを変更しないでください。
- Gitのcommitやpushは行わないでください。

## 実装時に確認すること

1. 開発時とPyInstaller実行時の両方で、正しい場所から`units.txt`を探せること。
2. `units.txt`が存在しない場合に標準単位だけで動作すること。
3. `units.txt`が不正な場合に警告を返し、標準単位で続行すること。
4. 次元が異なる変換・加減算を拒否すること。
5. `×`、`÷`、`^`を入力用記号として正規化すること。
6. 入力エラー時に入力内容を消さないこと。
7. GUIを生成しなくても計算エンジンをテストできること。

## 検証

仮想環境がなければ作成し、依存関係をインストールしてください。その後、少なくとも次を実行してください。

```powershell
py -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\python.exe -m compileall unit_calculator.py unit_engine.py
```

Windows以外の環境で作業している場合は、その環境に合う仮想環境のPythonを使って構いません。GUIを実際に表示できない環境では、計算エンジンの自動テストと、Tkinterモジュールの読み込み確認まで行い、未確認事項を最後に明記してください。

テストが失敗した場合は原因を修正して再実行してください。最後に、作成・変更したファイル、実行した検証、結果、残っている制約を簡潔に報告してください。

---
